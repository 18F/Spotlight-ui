# JavaScript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2.1
orbs: 
  cypress: cypress-io/cypress@1.23.0
workflows:
  build:
    jobs:
      - cypress/run:
        command: npm run test && npm run test:e2e:ci
        no-workspace: true
