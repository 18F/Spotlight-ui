import React from 'react'; // eslint-disable-line
import PropTypes from 'prop-types';

const AvailableFilters = (props) => {
    return (
        <div>AvailableFilters</div>
    );
};

AvailableFilters.propTypes = {};

export default AvailableFilters;

